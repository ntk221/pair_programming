desc: Generated by `runn new`
runners:
  req: 
    endpoint: http://localhost:8080
    useCookie: true
steps:

- req:
    /login:
      post:
        body:
          application/json:
            password: password123
            username: testuser
  test: |
    steps[0].res.status == 200
    && len(steps[0].res.cookies["session_id"].Value) > 0

- req:
    /task:
      post:
        body:
          application/json:
            task:
              detail: This is a new task description
              title: New Task
  test: |
    steps[1].res.status == 200
    && len(steps[1].res.rawBody) > 0

- req:
    /task/{{ previous.res.rawBody }}:
      get:
        body: null
  test: |
    steps[2].res.status == 200
    && current.res.body.detail == "This is a new task description"
    && current.res.body.title == "New Task"

- dump: previous.res
